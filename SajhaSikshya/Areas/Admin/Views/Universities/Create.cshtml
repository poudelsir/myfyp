@using SajhaSikshya.ViewModels.Admin.Catalog
@model UniversityFormViewModel
@{
    ViewData["Title"] = "Add University";
}

<div class="mb-3">
    <h1 class="h4 fw-bold mb-1">Add University</h1>
    <p class="text-secondary mb-0">Create a new university record.</p>
</div>

<div class="card" style="max-width: 640px;">
    <div class="card-body">
        <form asp-action="Create" method="post" novalidate>
            @if (!ViewData.ModelState.IsValid)
            {
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
            }
            <partial name="_Form" />
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
