@{
    // The Assistant is part of the marketplace-first, guest-reachable experience — it
    // always renders inside the public/marketplace nav, never a management shell,
    // regardless of the caller's role or where they clicked in from.
    Layout = "_MarketplaceLayout";
}
